# UI de la palette universelle — style DSFR approché en pur UNO awt.
# Ce package ne s'exécute que dans LibreOffice (imports uno) ; le moteur
# (core/) n'en dépend jamais, seul core/entry.py fait le pont.
